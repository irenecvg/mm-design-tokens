{
  "name": "mm-design-tokens",
  "version": "0.1.0",
  "description": "Design tokens, built with Style Dictionary. JSON source and output only — platform-specific formats (SCSS, etc.) are generated by consuming projects.",
  "type": "module",
  "private": true,
  "main": "build/tokens.json",
  "scripts": {
    "build": "style-dictionary build",
    "clean": "style-dictionary clean"
  },
  "keywords": ["design-tokens", "style-dictionary"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "style-dictionary": "^5.5.2"
  }
}
